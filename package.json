{
  "scripts": {
    "global": "token-transformer tokens.json theme/global.json global",
    "light": "token-transformer tokens.json theme/light.json global,light global",
    "dark": "token-transformer tokens.json theme/dark.json global,dark global"
  },
  "dependencies": {
    "token-transformer": "^0.0.33"
  }
}
